{
  "name": "@apiforge/template-function-prompt",
  "displayName": "Prompt Template Functions",
  "description": "Template functions for prompting for user input",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "apiforgecli build",
    "dev": "apiforgecli dev",
    "lint":"tsc --noEmit && eslint . --ext .ts,.tsx"
  }
}
