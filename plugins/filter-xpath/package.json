{
  "name": "@apiforge/filter-xpath",
  "displayName": "XPath Filter",
  "description": "Filter response XML data using XPath expressions",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "apiforgecli build",
    "dev": "apiforgecli dev",
    "lint":"tsc --noEmit && eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "xpath": "^0.0.34"
  }
}
