{
  "name": "@apiforge/template-function-encode",
  "displayName": "Encoding Template Functions",
  "description": "Template functions for encoding and decoding data",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "apiforgecli build",
    "dev": "apiforgecli dev",
    "lint":"tsc --noEmit && eslint . --ext .ts,.tsx"
  }
}
