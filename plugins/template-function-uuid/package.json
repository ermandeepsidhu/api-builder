{
  "name": "@apiforge/template-function-uuid",
  "displayName": "UUID Template Functions",
  "description": "Template functions for generating UUIDs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "apiforgecli build",
    "dev": "apiforgecli dev",
    "lint":"tsc --noEmit && eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  }
}
